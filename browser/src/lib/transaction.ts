import { Session } from "@/types/auth";
import { TransactionConfirmDate, TransactionQuoteDate } from "@/types/transaction";

async function quoteTransaction(session: Session, transactionQuoteDate: TransactionQuoteDate) {

}

async function confirmTransaction(session: Session, transactionConfirm: TransactionConfirmDate) {

}

async function getTransactionsByOwnerId(session: Session) {

}

async function getTransactionDetailByOwnerId(session: Session, transactionId: number) {

}